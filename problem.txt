Create a function within floodsystem/analysis.py that looks at the data from the 20 water stations with the highest current relative water level in England. Use a polynomial fit of order n, over the previous d days to predict the highest relative water level each station is predicted to reach at any point over the next y days (where n, d and y are all asked of the user when the code is run) using an analytical polynomial maximum. Return the names of the 5 stations with the highest predicted relative water level using the polynomial equation, alongside this predicted relative water level as an array of pairs (e.g. [["Place Name", "Water Level"], [...], ...]. Test this function on the mock dataset stored at collected_data/mock_data_28days.json.
